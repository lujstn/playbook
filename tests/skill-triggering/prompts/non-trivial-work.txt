We need to add a saved-searches feature to the app. Users should be able to name and store a search query from the results page, see their saved searches in a sidebar, and re-run any of them with one click. This needs a new database table with a migration, REST endpoints to create, list, rename and delete a saved search, the frontend sidebar component with its state wiring, and tests covering the API layer and the component. Please plan and build the whole thing end to end.
