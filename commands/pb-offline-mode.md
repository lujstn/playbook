---
description: Playbook offline mode: enable push notifications for long-running sessions.
---

Invoke the `playbook:offline-mode` skill. This is the canonical `/pb-offline-mode` form; use it when another tool defines a conflicting `/offline-mode` command.
