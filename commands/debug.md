---
description: Playbook debug mode: systematic investigation of an unknown failure.
---

Invoke the `playbook:debug-mode` skill. A bare `/debug` alias can be installed by `/playbook:setup`; this `/playbook:debug` form always works, and setup leaves the bare name alone if another tool already owns it.
