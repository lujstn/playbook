---
description: Playbook offline mode: enable push notifications for long-running sessions.
---

Invoke the `playbook:offline-mode` skill. Note: if another installed tool defines `/offline-mode`, the `/playbook:offline-mode` alias behaves identically.
